export class city {
    latitude: number;
    nome: string;
    longitude: number;
  
    constructor(nome: string, latitude: number , longitude: number) {
      this.nome = nome;
      this.latitude = latitude;
      this.longitude = longitude;
    
    }
  }
  